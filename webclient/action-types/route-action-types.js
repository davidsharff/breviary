'use strict';

const {TRANSITION, UPDATE_LOCATION} = require('react-router-redux');

const routeActionTypes = {
  TRANSITION: TRANSITION,
  UPDATE_LOCATION: UPDATE_LOCATION
};

module.exports = routeActionTypes;
